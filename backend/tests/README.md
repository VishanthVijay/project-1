# Backend Tests

This directory will hold automated tests for the backend API and service layers.

### Types of Tests
1. **Unit Tests**: Test individual utility functions and business logic (e.g. streak calculation).
2. **Integration Tests**: Test FastAPI endpoints using `TestClient` to ensure HTTP responses, database interactions, and status codes are correct.
