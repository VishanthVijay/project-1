# Database Migrations (Alembic)

This directory will hold database migration scripts generated by **Alembic**.

### What is Alembic?
Alembic is a database migration tool for SQLAlchemy.

### Purpose
When we change our SQLAlchemy models (e.g. adding a new column to a table), Alembic handles updating the database schema safely without losing existing data.
